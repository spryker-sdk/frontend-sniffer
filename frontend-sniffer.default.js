const application = require('./settings/core.global.application');
const style = require('./settings/core.global.style');
const component = require('./settings/core.component');

exports.core = {
    global: {
        application,
        style
    },

    component
}

exports.project = {}
